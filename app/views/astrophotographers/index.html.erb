
<%= link_to "Home", root_path %> |
<%= link_to "Constellations", constellations_path(@constellation) %> |
<%= link_to "Far away", far_away_constellations_path(@constellation) %>
<%= link_to "close", close_path(@constellation)%>

<div class ="">
<h1>All Astrophotographers</h1>
    <% @astrophotographers.each do |astro| %>
      <%= link_to astro.username.capitalize, astrophotographer_path(astro) %>
    <p><img src="<%= astro.profile_image %>"/></p>
  <% end %>


<%= link_to "back", astrophotographers_path(@astrophotographers) %> 

    <style>
    img {
    width: 400px;
    height: 400px;
    object-fit: scale-down;
   }

  </style> 